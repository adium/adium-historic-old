{
    IBClasses = (
        {CLASS = AIAutoScrollView; LANGUAGE = ObjC; SUPERCLASS = NSScrollView; }, 
        {CLASS = AIObject; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = AIWindowController; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = SHAutoValidatingTextView; LANGUAGE = ObjC; SUPERCLASS = NSTextView; }, 
        {
            ACTIONS = {acceptURL = id; cancel = id; closeWindow = id; removeURL = id; }; 
            CLASS = SHLinkEditorWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "button_Cancel" = NSButton; 
                "button_OK" = NSButton; 
                "imageView_invalidURLAlert" = NSImageView; 
                "scrollView_URL" = AIAutoScrollView; 
                target = id; 
                "textField_linkText" = NSTextField; 
                "textView_URL" = SHAutoValidatingTextView; 
            }; 
            SUPERCLASS = AIWindowController; 
        }
    ); 
    IBVersion = 1; 
}