{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {addFiles = id; setXtraType = id; }; 
            CLASS = MyDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                authorField = NSTextField; 
                fileView = NSTextView; 
                nameField = NSTextField; 
                tabs = NSTabView; 
                typePopup = NSPopUpButton; 
                versionField = NSTextField; 
            }; 
            SUPERCLASS = NSDocument; 
        }
    ); 
    IBVersion = 1; 
}