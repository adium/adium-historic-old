{
    IBClasses = (
        {CLASS = AICheckboxList; LANGUAGE = ObjC; SUPERCLASS = NSScrollView; }, 
        {
            ACTIONS = {preferenceChanged = id; tableClicked = id; }; 
            CLASS = AIEmoticonPreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "checkBox_enable" = NSButton; 
                "checkBox_enableEmoticon" = NSButton; 
                "image_emoticonImage" = NSImageView; 
                "table_curEmoticonTexts" = NSTableView; 
                "table_curEmoticons" = NSTableView; 
                "table_packList" = NSTableView; 
                "text_emoticonName" = NSTextField; 
                "text_emoticonPack" = NSTextField; 
                "text_packInfo" = NSTextView; 
                "view_prefView" = NSView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}