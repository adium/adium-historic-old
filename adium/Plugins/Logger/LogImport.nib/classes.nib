{
    IBClasses = (
        {
            ACTIONS = {closeWindow = id; importLogs = id; }; 
            CLASS = AILogImporter; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "panel_progress" = NSPanel; 
                "progress_working" = NSProgressIndicator; 
                "tableView_userList" = NSTableView; 
                "textField_Goal" = NSTextField; 
                "textField_Progress" = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}