{
    IBClasses = (
        {
            ACTIONS = {saveChanges = id; }; 
            CLASS = AIOscarAccountViewController; 
            LANGUAGE = ObjC; 
            OUTLETS = {"textField_handle" = NSTextField; "view_accountView" = NSView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}